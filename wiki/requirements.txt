# ArduConfigurator wiki — Sphinx + the Read-the-Docs theme (the same theme the
# ArduPilot wiki uses), so the layout/structure matches closely.
#
# PINNED on purpose: the built HTML (specifically sphinx_rtd_theme's nav-init
# inline <script>) must stay byte-stable, because the app's CSP
# (apps/web/public/_headers) allow-lists that script by sha256 hash. If you bump
# either version, rebuild the wiki and recompute that hash in _headers — otherwise
# the mobile-nav hamburger gets CSP-blocked on production.
sphinx==7.4.7
sphinx-rtd-theme==3.1.0
