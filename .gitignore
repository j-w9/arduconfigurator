node_modules
dist
.DS_Store
*.log
*.tsbuildinfo
playwright-report
test-results
apps/desktop/release
.vercel

# Isolated Web App build (signed bundle output + local signing key)
apps/web/dist-iwa/
apps/web/iwa/signing-key.pem

# Local audit / analysis notes — not part of the shipped repo
.audit/

# Generated ArduPilot parameter reference (wiki/tools/generate_parameter_reference.py).
# ~390 pages regenerated from the pinned wiki/data/*.json at build time — the
# source data is committed, the output is not.
wiki/parameters/group-*.rst
wiki/parameters/letter-*.rst
wiki/parameters/index.rst
wiki/_static/parameter-index.json

# Staged from the fork's sitl/ by scripts/stage-sitl.mjs before a build. The
# WebAssembly SITL binaries are committed in the fork, not here: this repo is
# source, and 7 MB of them per rebuild does not belong in its history.
apps/web/public/sitl/
